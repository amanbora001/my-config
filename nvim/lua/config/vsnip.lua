-- ~/.config/nvim/lua/plugins/vsnip.lua
return {
  "hrsh7th/vim-vsnip", -- Plugin name
  event = "InsertEnter", -- Load the plugin when entering insert mode
  config = function()
    -- Configure any plugin-specific settings here if needed
    -- Optionally you can set up mappings or snippets here
  end,
}

